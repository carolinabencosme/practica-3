FROM eclipse-temurin:21-jre-alpine

WORKDIR /app

CMD ["sh", "-c", "echo \"Simulator placeholder (${SIMULATOR_ID:-simulator}) publicando a ${JMS_BROKER_URL:-tcp://activemq:61616}.\" && tail -f /dev/null"]
