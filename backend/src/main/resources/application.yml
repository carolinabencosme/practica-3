spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/telemetry}
    username: ${SPRING_DATASOURCE_USERNAME:telemetry}
    password: ${SPRING_DATASOURCE_PASSWORD:telemetry}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  activemq:
    broker-url: ${SPRING_ACTIVEMQ_BROKER_URL:${JMS_BROKER_URL:tcp://localhost:61616}}
    user: ${SPRING_ACTIVEMQ_USER:${JMS_BROKER_USER:admin}}
    password: ${SPRING_ACTIVEMQ_PASSWORD:${JMS_BROKER_PASSWORD:admin}}
  jms:
    listener:
      acknowledge-mode: auto

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    root: INFO
    com.practica3.backend: INFO
